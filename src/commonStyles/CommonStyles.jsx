import styled from '@emotion/styled';

const SectionTitleStyled = styled.h2`
  font-weight: 400;
  font-size: 40px;
  line-height: 1.25;
  color: #f8f8f8;
  letter-spacing: 0.07em;
  margin-bottom: 90px;
`;

const styledComponents = { SectionTitleStyled };

export default styledComponents;
