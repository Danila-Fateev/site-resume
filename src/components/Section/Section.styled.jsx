import styled from '@emotion/styled';

const SectionStyled = styled.section`
  padding-top: 180px;
  padding-bottom: 180px;
`;

const styledComponents = { SectionStyled };

export default styledComponents;
